# Deploy

## Quick Deploy
Uses Anchor CLI to build and deploy Solana programs. Supports devnet, testnet, and mainnet-beta clusters.

## Configuration
Configure your Anchor.toml with cluster settings and program IDs.
